import { Application } from "express"

class StatsController {

}

export const statsController = (app: Application) => {
    const statsController = new StatsController()
    // TODO: Implement the routes
    // app.get('/', statsController.get)
    // app.post('/', statsController.post)
    // app.put('/', statsController.put)
    // app.delete('/', statsController.delete)
    // app.get('/:id', statsController.getOne)
}
